[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    clear_session: чистит сессию перед тестом. args: url сайта. kwargs: strategy=full (по умолчанию только куки)
